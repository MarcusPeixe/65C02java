.def offset 1
.org $0200

res:
irq:
nmi:
    LDA #$F0 ; load value into the accumulator
    Sta $da
    inc $5e ; times this code was run
    ldx #69 ; Load counter value into X
loop:
    sta offset,X ; Fill memory
    deX ; decrement counter
    bNe loop ; loop until counter is zero

    WAI ; end